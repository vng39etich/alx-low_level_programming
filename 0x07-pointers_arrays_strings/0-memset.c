#include "main.h"

/**
 * _memset - fill the memory with a byte
 * @s: memory to fill
 * @b: constant to fill
 * @n: memory bytes to fill
 *
 * return: filled memory area
 */
char *_memset(char *s, char b, unsigned int n)
{
	unsigned int a;

	for (a = 0; a < n; a++)
	{
		s[a] = b
	}

}
