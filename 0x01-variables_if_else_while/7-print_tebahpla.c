#include <stdio.h>

/**
 * main - Prints the lowercase alphabet in reverse.
 * Return: Always 0.
 */
int main(void)

{
		char alp;

		for (apl = 'z'; alp >= 'a'; apl--)
		putchar(apl);

		putchar('\n');

		return (0);
}
