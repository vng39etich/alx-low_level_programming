#include <stdio.h>

/**
 * main - Prints the lowercase alphabet in reverse.
 * Return: Always 0.
 */
int main(void)

{
		char alph;

		for (aplh = 'z'; alph >= 'a'; aplh--)
		putchar(aplh);

		putchar('\n');

		return (0);
}
